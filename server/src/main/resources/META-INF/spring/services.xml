<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="file:${app.config}"/>
    <context:component-scan base-package="org.hejki.heklasys">
        <context:include-filter type="assignable" expression="org.springframework.data.repository.Repository"/>
    </context:component-scan>

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <bean id="sqlGenerator" class="com.blogspot.nurkiewicz.jdbcrepository.sql.PostgreSqlGenerator"/>

    <bean id="threadPoolExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolExecutorFactoryBean">
        <property name="corePoolSize" value="${thread.corePoolSize}"/>
        <property name="keepAliveSeconds" value="${thread.keepAlive}"/>
        <property name="maxPoolSize" value="${thread.maxPoolSize}"/>
        <property name="queueCapacity" value="${thread.maxPoolSize}"/>
        <property name="threadGroupName" value="DefaultThreadPool"/>
        <property name="threadNamePrefix" value="udp_"/>
    </bean>

    <bean class="org.springframework.beans.factory.config.CustomEditorConfigurer">
        <property name="customEditors">
            <map>
                <entry key="java.net.InetAddress" value="org.hejki.sys.core.beans.InetAddressPropertyEditor"/>
            </map>
        </property>
    </bean>

    <bean class="org.hejki.heklasys.msg.service.MessageCommunicationServiceImpl"/>
</beans>
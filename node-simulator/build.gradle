apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile(
            'ch.qos.logback:logback-classic:1.1.0',
    )
}

configurations {
    all*.exclude group: 'commons-logging'
}

compileJava {
    sourceCompatibility = "1.8"
}